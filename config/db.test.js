const MongoClient = require("mongodb").MongoClient;
const DBObj = require('./db');

describe('mongoDB connection', () => { 

  test('the app connect successfully && return the DB', () => { 

  })
  
  test('the app connect successfully && return the DB', () => { 

  })
})